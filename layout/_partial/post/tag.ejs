<% if (post.tags && post.tags.length){ %>
  <span class="article-tag">
    <i class="fa fa-tag"></i>
	<%- list_tags(post.tags, {
	   show_count: false,
	   style: 'link',
	   class: 'article-tag'
	}) %>
  </span>
<% } %>
