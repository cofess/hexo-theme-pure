<% if (typeof(script) !== 'undefined' && script) { %>
    <% if (page.layout === 'repository'){ %>
        <%- partial('_script/repository') %>
    <% } %>
    <% if (page.layout === 'books'){ %>
        <%- partial('_script/douban') %>
    <% } %>
    <% if (theme.search.insight) { %>
        <%- partial('_script/_search/insight') %>
    <% } %>
    <% if (theme.search.baidu) { %>
        <%- partial('_script/_search/baidu') %>
    <% } %>
    <% if (is_home() || is_post()) { %>
        <%- partial('_script/pv') %>
    <% } %>
    <% if (!is_home() && !is_category() && !is_tag() && !is_archive() && post.comments) { %>
        <%- partial('_script/_comment/script') %>
    <% } %>
    <%- partial('_script/_analytics/script') %>
<% } else { %>
    <% if (page.mathjax) { %>
        <link href="https://cdn.bootcss.com/KaTeX/0.7.1/katex.min.css" rel="stylesheet">
    <% } %>
    <% if (theme.comment.type === 'gitment' && !is_home() && !is_category() && !is_tag() && !is_archive() && post.comments) { %>
        <link rel="stylesheet" href="https://imsun.github.io/gitment/style/default.css">
    <% } %>
<% } %>